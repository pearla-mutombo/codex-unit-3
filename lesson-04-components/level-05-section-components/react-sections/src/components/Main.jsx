export function Main() {
  return (
    <main>
      <p>
        To convert sections into React Components is the process of breaking
        HTML Structure into smaller reusable pieces. We do this by creating a
        new jsx file for each section. by exporting the components and importing
        it into your main app file.
      </p>
    </main>
  );
}
