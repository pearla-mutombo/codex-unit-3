export default function Main() {
  return (
    <main>
      <p>
        Semantic Template is to use native semantic elements such as header,
        main, nav, and footer. We would wrap in react fragment so we don't
        pollute the DOM with unnecessary structural div elements.
      </p>
    </main>
  );
}
