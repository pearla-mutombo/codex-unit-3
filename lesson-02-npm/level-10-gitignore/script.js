console.log(
  "gitignore, we use it to ignore files that we do not want to push to GitHub, for example 'node_modelues'. And secret token, such as 'API Keys', we don't want other to access our secured and sensitive information. Basically anything that you don't want in the cloud you ignore it. you don't push it.",
);
