const colors = [ "Red", "Orange", "Yellow", "Green", "Blue", "Indigo", "Violet", "Cyan", "Teal", "Pink"];
for ( let i = 0 ; i < colors.length ; i++ ) {
    console.log(`color${i + 1}: ${colors[i]}`);
}